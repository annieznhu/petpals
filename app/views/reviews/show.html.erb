<div class='all-my-reviews'>
  <h1>Tous mes avis</h1>
  <div>
    <% current_user.reviews.each do |review| %>

      <div class='card-avis'>
      <br>
      Mon commentaire :
        <p><%=review.place.name%></p>
        <%= review.content %>
        Je donne une note de <%= review.rating %> sur 5 à ce parc.
      <%= link_to "Delete", delete_review_path(@reviews), :confirm => "Are you sure?", :method => :delete %>
      </div>
      <br>
    <% end %>
  </div>
</div>
